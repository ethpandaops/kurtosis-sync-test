participants:
  - el_type: ${EL_CLIENT_TYPE}
    el_image: ${EL_CLIENT_IMAGE}
    cl_type: ${CL_CLIENT_TYPE}
    cl_image: ${CL_CLIENT_IMAGE}
    supernode: ${SUPERNODE_ENABLED}
network_params:
  network: ${DEVNET}
  devnet_repo: ${DEVNET_REPO}
persistent: true
additional_services:
  - assertoor
checkpoint_sync_enabled: ${CHECKPOINT_SYNC}

docker_cache_params:
  enabled: true
  url: "docker.ethquokkaops.io"
